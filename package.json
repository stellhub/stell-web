{
  "name": "stellar-axis-docs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "VitePress site for Stell Hub documentation.",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "docs:i18n": "node scripts/generate-bilingual-docs.mjs",
    "docs:validate": "node scripts/validate-topics.mjs",
    "docs:check": "npm run docs:i18n && npm run docs:validate && vitepress build docs",
    "docs:dev": "npm run docs:i18n && vitepress dev docs",
    "docs:build": "npm run docs:i18n && vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "1.6.4"
  }
}
